import { FunctionCall, parseProg } from "../src/parseProg";
// import { tokenArray } from "../src/token";
import { createTokenizer } from "../src/Tokenizer";
import { refResolver } from "../src/refResolver";
describe("refResolver", () => {
  it(" definition ", () => {
    // const prog = parseProg(createTokenizer(tokenArray));
    // refResolver(prog);
    // // prog[1] functionCall
    // expect((prog.stmts[1] as FunctionCall).definition).toBeTruthy();
  });
});
